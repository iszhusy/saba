# Safety Constraints — 模块目标

**目的**：产出 risk floor、必说/禁说动作与 warning signals，供 Executive synthesis 注入；**不**直接写最终用户全文。

**成功标准**：输出 `SafetyConstraints`；复用现有规则检测；逐步弃用 `corrected.*` 主路径。

**行为规格**：[behavior/06-safety-constraints.md](../../behavior/06-safety-constraints.md)
